<views:MvxPhonePage 
    x:Class="TekConf.UI.WinPhone.Views.SessionDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:views="clr-namespace:Cirrious.MvvmCross.WindowsPhone.Views;assembly=Cirrious.MvvmCross.WindowsPhone"
		mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  
		Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

	<!--LayoutRoot is the root grid where all page content is placed-->
	<Grid x:Name="LayoutRoot" Background="Transparent">
		<!--Pivot Control-->
		<controls:Pivot Title="{Binding PageTitle}">

			<!--Pivot item one-->
			<controls:PivotItem>
				<controls:PivotItem.Header>
					<TextBlock Text="detail" Foreground="#FF81994D" />
				</controls:PivotItem.Header>
				<ScrollViewer>
					<StackPanel>
						<TextBlock Text="{Binding Session.title}" FontSize="38" TextWrapping="Wrap" Margin="0,10,0,0"></TextBlock>

						<TextBlock Text="{Binding Session.description}" TextWrapping="Wrap" Margin="0,10,0,0"></TextBlock>
					</StackPanel>
				</ScrollViewer>
			</controls:PivotItem>

			<!--Pivot item two-->
			<controls:PivotItem Header="speakers">
				<Grid/>
			</controls:PivotItem>
		</controls:Pivot>
	</Grid>

</views:MvxPhonePage>
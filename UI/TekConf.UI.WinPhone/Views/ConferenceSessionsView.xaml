<views:MvxPhonePage 
    x:Class="TekConf.UI.WinPhone.Views.ConferenceSessionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:views="clr-namespace:Cirrious.MvvmCross.WindowsPhone.Views;assembly=Cirrious.MvvmCross.WindowsPhone"
		mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

	<!--LayoutRoot is the root grid where all page content is placed-->
	<Grid x:Name="LayoutRoot" Background="Transparent">
		<!--Pivot Control-->
		<controls:Pivot>
			<controls:Pivot.TitleTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding PageTitle}" Foreground="#FF81994D" />
				</DataTemplate>
			</controls:Pivot.TitleTemplate>
			
			<controls:PivotItem>
				<controls:PivotItem.Header>
					<TextBlock Text="favorites" Foreground="#FF81994D" />
				</controls:PivotItem.Header>
				<Grid x:Name="MyFavoritesGrid" Grid.Row="1" Margin="12,0,12,0">
					<ListBox ItemsSource="{Binding Conference.sessions}">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Vertical" Margin="0,0,0,10">
									<TextBlock Text="{Binding title}" FontSize="28" TextWrapping="Wrap" SizeChanged="SessionTitle_OnSizeChanged"></TextBlock>
									<TextBlock Text="{Binding startDescription}" FontSize="20"></TextBlock>
									<TextBlock Text="{Binding room}" FontSize="20"></TextBlock>
								</StackPanel>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</Grid>
			</controls:PivotItem>

			<!--Pivot item two-->
			<controls:PivotItem>
				<controls:PivotItem.Header>
					<TextBlock Text="time" Foreground="#FF81994D" />
				</controls:PivotItem.Header>
				<Grid x:Name="SessionsByTimeGrid" Grid.Row="1" Margin="12,0,12,0">
					<ListBox ItemsSource="{Binding Conference.SessionsByTime}">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Vertical" Margin="0,0,0,10">
									<TextBlock Text="{Binding title}" FontSize="28" TextWrapping="Wrap" SizeChanged="SessionTitle_OnSizeChanged"></TextBlock>
									<TextBlock Text="{Binding startDescription}" FontSize="20"></TextBlock>
									<TextBlock Text="{Binding room}" FontSize="20"></TextBlock>
								</StackPanel>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</Grid>
			</controls:PivotItem>

			<controls:PivotItem>
				<controls:PivotItem.Header>
					<TextBlock Text="title" Foreground="#FF81994D" />
				</controls:PivotItem.Header>
				<Grid x:Name="SessionsByTitleGrid" Grid.Row="1" Margin="12,0,12,0">
					<ListBox ItemsSource="{Binding Conference.sessions}">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Vertical" Margin="0,0,0,10">
									<TextBlock Text="{Binding title}" FontSize="28" TextWrapping="Wrap" SizeChanged="SessionTitle_OnSizeChanged"></TextBlock>
									<TextBlock Text="{Binding startDescription}" FontSize="20"></TextBlock>
									<TextBlock Text="{Binding room}" FontSize="20"></TextBlock>
								</StackPanel>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</Grid>
			</controls:PivotItem>

			<controls:PivotItem>
				<controls:PivotItem.Header>
					<TextBlock Text="speaker" Foreground="#FF81994D" />
				</controls:PivotItem.Header>
				<Grid x:Name="SessionsBySpeakerGrid" Grid.Row="1" Margin="12,0,12,0">
					<ListBox ItemsSource="{Binding Conference.sessions}">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Vertical" Margin="0,0,0,10">
									<TextBlock Text="{Binding title}" FontSize="28" TextWrapping="Wrap" SizeChanged="SessionTitle_OnSizeChanged"></TextBlock>
									<TextBlock Text="{Binding startDescription}" FontSize="20"></TextBlock>
									<TextBlock Text="{Binding room}" FontSize="20"></TextBlock>
								</StackPanel>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</Grid>
			</controls:PivotItem>

			<controls:PivotItem>
				<controls:PivotItem.Header>
					<TextBlock Text="tag" Foreground="#FF81994D" />
				</controls:PivotItem.Header>
				<Grid x:Name="SessionsByTagGrid" Grid.Row="1" Margin="12,0,12,0">
					<ListBox ItemsSource="{Binding Conference.sessions}">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Vertical" Margin="0,0,0,10">
									<TextBlock Text="{Binding title}" FontSize="28" TextWrapping="Wrap" SizeChanged="SessionTitle_OnSizeChanged"></TextBlock>
									<TextBlock Text="{Binding startDescription}" FontSize="20"></TextBlock>
									<TextBlock Text="{Binding room}" FontSize="20"></TextBlock>
								</StackPanel>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</Grid>
			</controls:PivotItem>

			<controls:PivotItem>
				<controls:PivotItem.Header>
					<TextBlock Text="room" Foreground="#FF81994D" />
				</controls:PivotItem.Header>
				<Grid x:Name="SessionsByRoomGrid" Grid.Row="1" Margin="12,0,12,0">
					<ListBox ItemsSource="{Binding Conference.sessions}">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Vertical" Margin="0,0,0,10">
									<TextBlock Text="{Binding title}" FontSize="28" TextWrapping="Wrap" SizeChanged="SessionTitle_OnSizeChanged"></TextBlock>
									<TextBlock Text="{Binding startDescription}" FontSize="20"></TextBlock>
									<TextBlock Text="{Binding room}" FontSize="20"></TextBlock>
								</StackPanel>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</Grid>
			</controls:PivotItem>
		</controls:Pivot>
	</Grid>

</views:MvxPhonePage>
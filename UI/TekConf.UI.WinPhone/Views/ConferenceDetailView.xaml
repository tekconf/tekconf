<views:MvxPhonePage 
    x:Class="TekConf.UI.WinPhone.Views.ConferenceDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:views="clr-namespace:Cirrious.MvvmCross.WindowsPhone.Views;assembly=Cirrious.MvvmCross.WindowsPhone"
	mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

	<!--LayoutRoot is the root grid where all page content is placed-->
	<Grid x:Name="LayoutRoot" Background="Transparent">
		<!--Pivot Control-->
		<controls:Pivot Title="TEKCONF">
			<!--Pivot item one-->
			<controls:PivotItem Header="detail">
				<Grid>
					<StackPanel>
						<Image Height="180" Width="260" Source="{Binding Conference.imageUrl}"></Image>

						<TextBlock Text="{Binding Conference.description}" TextWrapping="Wrap"></TextBlock>
					</StackPanel>
				</Grid>
			</controls:PivotItem>

			<!--Pivot item two-->

			<controls:PivotItem Header="sessions">
				<Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
					<ListBox ItemsSource="{Binding Conference.sessions}">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<!--<Image Height="180" Width="260" Source="{Binding imageUrl}"></Image>-->
									<StackPanel>
										<TextBlock Text="{Binding title}" FontSize="36"></TextBlock>
										<!--<TextBlock Text="{Binding tagline}" FontSize="12"></TextBlock>-->
									</StackPanel>
								</StackPanel>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</Grid>
			</controls:PivotItem>


		</controls:Pivot>
	</Grid>

</views:MvxPhonePage>